name: Deploy to Azure Web App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Configurar Git
        run: |
          git config --global user.email "github-actions@bot.com"
          git config --global user.name "GitHub Actions"

      - name: Autenticarse y hacer push a Azure
        env:
          AZURE_USERNAME: jf2021070309
          AZURE_PASSWORD: ${{ secrets.AZURE_PASSWORD }}
        run: |
          echo "echo $AZURE_PASSWORD" > git-askpass.sh
          chmod +x git-askpass.sh
          export GIT_ASKPASS=./git-askpass.sh
          git remote set-url origin https://$AZURE_USERNAME@webapp-proyecto-patrones.scm.azurewebsites.net/webapp-proyecto-patrones.git
          git push origin main:master
