name: Deploy to Azure Web App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Configurar Git
        run: |
          git config --global user.email "github-actions@bot.com"
          git config --global user.name "GitHub Actions"

      - name: Hacer push a Azure con autenticación embebida
        env:
          AZURE_USERNAME: $webapp-proyecto-patrones
          AZURE_PASSWORD: gNbfkGxawf4fh0sopa0uA9qLPk9odmn1SXSLAqr3n3ezueDNauoGslvxvd14
        run: |
          REMOTE_URL="https://${AZURE_USERNAME}:${AZURE_PASSWORD}@webapp-proyecto-patrones.scm.azurewebsites.net/webapp-proyecto-patrones.git"
          git remote set-url origin "$REMOTE_URL"
          git push origin main:master --force
